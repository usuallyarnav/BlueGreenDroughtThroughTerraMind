# GWSat v3 — Requirements
# Python 3.10+ recommended
#
# Install order:
#   1.  pip install -r requirements.txt
#   2.  pip install terratorch        (or from source — see below)
#   3.  Verify:  python model.py      (should print TerraMind loaded)

torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0

# Primary path (IBM official). Install ONE of:
#   pip install terratorch
#   pip install git+https://github.com/IBM/terratorch.git
#
transformers>=4.35.0
huggingface_hub>=0.19.0
timm>=0.9.0
rasterio>=1.3.0
pandas>=2.0.0
Pillow>=10.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0
gradio>=4.0.0
matplotlib>=3.7.0
onnx>=1.15.0
onnxruntime>=1.15.0